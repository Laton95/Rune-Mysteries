package com.laton95.test.block;

import java.util.Random;

import com.laton95.test.init.ItemRegistry;

import net.minecraft.block.material.Material;
import net.minecraft.block.state.IBlockState;
import net.minecraft.creativetab.CreativeTabs;
import net.minecraft.init.Items;
import net.minecraft.item.Item;

public class BlockRune_Essence extends BlockTest{

	public BlockRune_Essence() {
		super("rune_Essence_Block", CreativeTabs.BUILDING_BLOCKS, Material.ROCK,  1.5f, "pickaxe", 1);
	}
	
	public Item getItemDropped(IBlockState state, Random rand, int fortune)
    {
		return rand.nextInt(10 - fortune * 3) == 0 ? ItemRegistry.runeEssence : super.getItemDropped(state, rand, fortune);
        
		/*if (fortune > 3)
        {
            fortune = 3;
        }

        return rand.nextInt(10 - fortune * 3) == 0 ? Items.FLINT : super.getItemDropped(state, rand, fortune);*/
    }
}
